<UserControl x:Class="ProGlassAutomation.Views.Optimization.OptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1300"
             Background="#F8FAFC"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#10B981"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#EF4444"/>
        <SolidColorBrush x:Key="BorderSoft" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- ✅ FIX: BorderBrush uses Value, not Color -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="#2563EB"/>
            <Setter Property="SelectionBrush" Value="#2563EB"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="1200" Margin="32" HorizontalAlignment="Center">

            <!-- HEADER + ACTIONS -->
            <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,16" BorderBrush="#E5E7EB" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Smart Glass Optimization" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Precision 2D nesting · 0° and 90° complex fitting" FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Manage Stock Sheets" Click="OpenStockDialog_Click" Background="White" Foreground="#0F172A" BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,0,8,0"/>
                        <Button Content="Manage Parts" Click="OpenPartsDialog_Click" Background="White" Foreground="#0F172A" BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,0,8,0"/>
                        <Button Content="View 2D Layout" Click="OpenLayoutDialog_Click" Background="#10B981" Foreground="White" Margin="0,0,8,0"/>
                        <Button Content="View Report" Click="OpenReportDialog_Click" Background="#0F172A" Foreground="White" Margin="0,0,8,0"/>
                        <Button Content="Run Optimization" Click="RunOptimization_Click" Background="#2563EB" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- KPI CARDS — bound to ViewModel -->
            <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
                <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,8,0" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="STOCK SHEETS" FontSize="10" FontWeight="Bold" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding StockSheetCount, Mode=OneWay, StringFormat=N0}"
                                   FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="12" Padding="20" Margin="4,0" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="PARTS TO CUT" FontSize="10" FontWeight="Bold" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding PartCount, Mode=OneWay, StringFormat=N0}"
                                   FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="12" Padding="20" Margin="4,0" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="UTILIZATION" FontSize="10" FontWeight="Bold" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding Utilization, Mode=OneWay, StringFormat={}{0:0.##}%}"
                                   FontSize="28" FontWeight="Bold" Foreground="#10B981" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="12" Padding="20" Margin="8,0,0,0" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="WASTE" FontSize="10" FontWeight="Bold" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding Waste, Mode=OneWay, StringFormat={}{0:0.##}%}"
                                   FontSize="28" FontWeight="Bold" Foreground="#EF4444" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- SETTINGS -->
            <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,16" BorderBrush="#E5E7EB" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Cutting Settings" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,12"/>
                    <UniformGrid Columns="4" Rows="1">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="KERF (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtKerf" Text="4"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="TRIM (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtTrim" Text="10"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="BREAKOUT LEFT (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtBreakLeft" Text="15"/>
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="BREAKOUT RIGHT (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtBreakRight" Text="15"/>
                        </StackPanel>
                    </UniformGrid>
                    <UniformGrid Columns="3" Rows="1" Margin="0,12,0,0">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="BREAKOUT TOP (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtBreakTop" Text="15"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="BREAKOUT BOTTOM (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtBreakBottom" Text="15"/>
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="MIN BREAKOUT (MM)" FontSize="10" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtBreakMin" Text="15"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- ROTATION -->
            <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,16" BorderBrush="#E5E7EB" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Rotation Mode" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,12"/>
                    <UniformGrid Columns="3" Rows="1">
                        <RadioButton x:Name="rbRot0" Content="0° Only — Keep exact orientation" GroupName="Rotation" Margin="0,0,8,0" Checked="Rotation_Changed"/>
                        <RadioButton x:Name="rbRot90" Content="90° Only — Force 90° rotation" GroupName="Rotation" Margin="4,0" Checked="Rotation_Changed"/>
                        <RadioButton x:Name="rbRotComplex" Content="Complex 0° + 90° — Automatic best fit" GroupName="Rotation" IsChecked="True" Margin="8,0,0,0" Checked="Rotation_Changed"/>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- QUICK ACTIONS — empty state with proper DataTrigger -->
            <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,16" BorderBrush="#E5E7EB" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Quick Actions" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,12"/>
                    <TextBlock x:Name="txtEmptyState"
                               Text="No stock or parts added yet. Click 'Manage Stock Sheets' or 'Manage Parts' to begin, or import from ProformaInvoice."
                               FontSize="13" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,0,0,12">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasContent}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Button Content="Import from ProformaInvoice" Click="ImportFromProforma_Click" Background="White" Foreground="#0F172A" BorderBrush="#E5E7EB" BorderThickness="1" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>