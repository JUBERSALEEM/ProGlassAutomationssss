<UserControl x:Class="ProGlassAutomation.Views.ProformaInvoice.ProformaInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ProGlassAutomation.Converters"
             Background="White">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <converters:BoolToColorGreenConverter x:Key="BoolToColorGreen"/>
        <converters:BoolToBrushGreenConverter x:Key="BoolToBrushGreen"/>
        <converters:BoolToBrushGreenBorderConverter x:Key="BoolToBrushGreenBorder"/>

        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="GreenGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#10B981" Offset="0"/>
            <GradientStop Color="#059669" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BlueGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#3B82F6" Offset="0"/>
            <GradientStop Color="#2563EB" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PurpleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8B5CF6" Offset="0"/>
            <GradientStop Color="#7C3AED" Offset="1"/>
        </LinearGradientBrush>

        <!-- COLORS -->
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="PrimaryBlueDark" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryBlueLight" Color="#DBEAFE"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessGreenLight" Color="#D1FAE5"/>
        <SolidColorBrush x:Key="WarningRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="WarningRedLight" Color="#FEE2E2"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SidebarBackground" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="TextMuted" Color="#94A3B8"/>

        <!-- CARD STYLE -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.05" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- BUTTONS -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlueDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource WarningRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DC2626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- INPUTS -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <EventSetter Event="GotFocus" Handler="TextBox_GotFocus"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <EventSetter Event="GotFocus" Handler="TextBox_GotFocus"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>

        <!-- DATA GRID -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryBlueDark}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="3,4"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <!-- LABELS -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SmallLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="8"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="Margin" Value="0,3,0,3"/>
        </Style>

        <!-- MODULE TOGGLE -->
        <Style x:Key="ModuleToggleStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WorkTypeToggleStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SuccessGreen}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SuccessGreen}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SuccessGreenLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ==================== LEFT - PROFORMA INVOICE ==================== -->
        <Grid Grid.Column="0" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="INVOICE:" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding Invoice.InvoiceNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SmallInputStyle}" MinWidth="120" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource SecondaryButtonStyle}" Content="TOGGLE LM" Command="{Binding ToggleLMCommand}" Margin="0,0,6,0"/>
                        <Button Style="{StaticResource SecondaryButtonStyle}" Content="PREVIEW" Click="PrintPreview_Click" Margin="0,0,6,0"/>
                        <Button Style="{StaticResource PrimaryButtonStyle}" Content="PRINT" Command="{Binding PrintCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Header -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F8FAFC" CornerRadius="10" Padding="14" BorderBrush="{StaticResource BorderLight}" BorderThickness="1" Margin="0,0,10,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBox Grid.Row="0" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" BorderThickness="0" Background="Transparent" Padding="0" Margin="0,0,0,8"/>

                                    <Border Grid.Row="1" HorizontalAlignment="Left" Background="{StaticResource PrimaryBlueLight}" CornerRadius="6" Padding="8,4" Margin="0,0,0,8" BorderBrush="#BFDBFE" BorderThickness="1">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="TRN:" FontSize="9" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBox Text="{Binding CompanyTRN, UpdateSourceTrigger=PropertyChanged}" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlueDark}" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Background="{StaticResource SuccessGreenLight}" CornerRadius="20" Padding="6,4" Margin="0,0,6,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📍" FontSize="10" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                                <TextBox Text="{Binding CompanyLocation, UpdateSourceTrigger=PropertyChanged}" FontSize="9" Foreground="#166534" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Center" MinWidth="60"/>
                                            </StackPanel>
                                        </Border>

                                        <Border Grid.Column="2" Background="{StaticResource WarningRedLight}" CornerRadius="20" Padding="6,4">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📞" FontSize="10" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                                <TextBox Text="{Binding CompanyPhone, UpdateSourceTrigger=PropertyChanged}" FontSize="9" Foreground="#991B1B" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Center" MinWidth="80"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Border Background="{StaticResource PrimaryBlueDark}" CornerRadius="10" Padding="18,10" Margin="0,0,0,10">
                                    <TextBlock Text="PROFORMA INVOICE" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                </Border>

                                <Border Background="#F8FAFC" CornerRadius="8" Padding="12,8" BorderBrush="{StaticResource BorderLight}" BorderThickness="1">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="📅" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="Date:" FontSize="9" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <DatePicker SelectedDate="{Binding Invoice.InvoiceDate}" Width="100"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⏰" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="Valid:" FontSize="9" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <DatePicker SelectedDate="{Binding Invoice.ValidUntil}" Width="100"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Customer & Project & Other -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="CUSTOMER DETAILS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" Margin="0,0,0,6"/>

                                <TextBlock Text="Company Name *" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.CustomerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,4"/>

                                <TextBlock Text="TRN Number" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.CustomerTRN, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,4"/>

                                <TextBlock Text="Address" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.CustomerAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" TextWrapping="Wrap" AcceptsReturn="True" Height="40"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="PROJECT DETAILS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" Margin="0,0,0,6"/>

                                <TextBlock Text="Project Name *" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.ProjectName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,4"/>

                                <TextBlock Text="Location *" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.ProjectLocation, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="OTHER DETAILS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" Margin="0,0,0,6"/>

                                <TextBlock Text="LPO No." Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.LPONo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,4"/>

                                <TextBlock Text="Attention" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.AttentionName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,4"/>

                                <TextBlock Text="Contact No." Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding Invoice.ContactNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Specifications Header -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="SPECIFICATIONS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Style="{StaticResource PrimaryButtonStyle}" Content="+ ADD SPEC" Command="{Binding AddSpecificationCommand}" Margin="0,0,4,0" Padding="8,5"/>
                            <Button Style="{StaticResource DangerButtonStyle}" Content="- REMOVE" Command="{Binding RemoveSpecificationCommand}" Padding="8,5"/>
                        </StackPanel>
                    </Grid>

                    <!-- Specifications List -->
                    <ItemsControl ItemsSource="{Binding Invoice.Specifications}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" CornerRadius="10" BorderBrush="{StaticResource PrimaryBlueDark}" BorderThickness="1.5" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Spec Header -->
                                        <Border Grid.Row="0" Background="{StaticResource PrimaryBlueDark}" CornerRadius="9,9,0,0" Padding="10,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="SPEC:" FontSize="9" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                <TextBox Grid.Column="1" Text="{Binding SpecificationName}" Background="White" FontSize="9" FontWeight="SemiBold" MinWidth="80" Padding="4,2" VerticalAlignment="Center"/>

                                                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0">
                                                    <TextBlock Text="BASE:" FontSize="8" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                                    <TextBox Text="{Binding BasePrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="55" Padding="4,2" FontSize="9" Background="#FEF9C3" FontWeight="Bold"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="0,0,8,0">
                                                    <TextBlock Text="SUR%:" FontSize="8" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                                    <TextBox Text="{Binding SurchargePercent, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Width="40" Padding="4,2" FontSize="9" Background="#FEF9C3" FontWeight="Bold"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="0,0,8,0">
                                                    <TextBlock Text="THR:" FontSize="8" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                                    <TextBox Text="{Binding SurchargeThreshold, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="40" Padding="4,2" FontSize="9" Background="#FEF9C3" FontWeight="Bold"/>
                                                </StackPanel>

                                                <Button Grid.Column="5" Content="+ ROW" Style="{StaticResource SecondaryButtonStyle}" Click="AddRow_Click" Tag="{Binding}" Padding="8,4"/>
                                            </Grid>
                                        </Border>

                                        <!-- DataGrid -->
                                        <Border Grid.Row="1" BorderBrush="{StaticResource BorderLight}" BorderThickness="1,0,1,1">
                                            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" BorderThickness="0" Background="White" ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}" RowStyle="{StaticResource DataGridRowStyle}" GridLinesVisibility="All" HorizontalGridLinesBrush="{StaticResource BorderLight}" VerticalGridLinesBrush="{StaticResource BorderLight}" HeadersVisibility="Column" RowHeight="28" MinHeight="56" SelectionUnit="Cell" FocusVisualStyle="{x:Null}" PreviewKeyDown="DataGrid_PreviewKeyDown">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="SR" Binding="{Binding SrNo}" Width="35" IsReadOnly="True">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Background" Value="#F1F5F9"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="GLASS REF" Binding="{Binding GlassRef}" Width="90">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="Padding" Value="2,0"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="Padding" Value="2,0"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="W1" Binding="{Binding Width1, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="55">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="TextAlignment" Value="Center"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="H1" Binding="{Binding Height1, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="55">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="TextAlignment" Value="Center"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="W2" Binding="{Binding Width2, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="55">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="TextAlignment" Value="Center"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="H2" Binding="{Binding Height2, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="55">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="TextAlignment" Value="Center"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="QTY" Binding="{Binding Qty, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="45">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.EditingElementStyle>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="TextAlignment" Value="Center"/>
                                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                            </Style>
                                                        </DataGridTextColumn.EditingElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="SQM" Binding="{Binding SQM, StringFormat=N4}" Width="60" IsReadOnly="True">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="Background" Value="#E0F2FE"/>
                                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="TOTAL SQM" Binding="{Binding TotalSQM, StringFormat=N4}" Width="70" IsReadOnly="True">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="Background" Value="#BAE6FD"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="PRICE" Binding="{Binding DisplayPrice, StringFormat=N2}" Width="65" IsReadOnly="True">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="Padding" Value="0,0,3,0"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBlueDark}"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTextColumn Header="TOTAL" Binding="{Binding TotalPrice, StringFormat=N2}" Width="75" IsReadOnly="True">
                                                        <DataGridTextColumn.ElementStyle>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Padding" Value="0,0,3,0"/>
                                                                <Setter Property="Background" Value="#FEF3C7"/>
                                                                <Setter Property="FontSize" Value="8"/>
                                                            </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                    </DataGridTextColumn>

                                                    <DataGridTemplateColumn Header="" Width="35">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <Button Content="X" FontSize="9" FontWeight="Bold" Foreground="{StaticResource WarningRed}" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="0" Click="DeleteRow_Click" Tag="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" Focusable="False"/>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </Border>

                                        <!-- Spec Totals -->
                                        <Border Grid.Row="2" Background="#F1F5F9" CornerRadius="0,0,9,9" Padding="10,6">
                                            <Border BorderBrush="{StaticResource BorderLight}" BorderThickness="0,1,0,0" Padding="0,6,0,0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Text="SPEC TOTAL" FontSize="8" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" VerticalAlignment="Center"/>

                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0">
                                                        <TextBlock Text="Qty:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SpecTotalQty}" FontSize="8" FontWeight="Bold" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,16,0">
                                                        <TextBlock Text="SQM:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SpecTotalSQM, StringFormat=N4}" FontSize="8" FontWeight="Bold" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="0,0,16,0">
                                                        <TextBlock Text="LM:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SpecTotalLM, StringFormat=N4}" FontSize="8" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="4" Orientation="Horizontal">
                                                        <TextBlock Text="AED:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SpecTotalPrice, StringFormat=N2}" FontSize="9" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Grand Totals -->
                    <Border Background="{StaticResource PrimaryBlueDark}" CornerRadius="10" Padding="14" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="240"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="TERMS &amp; CONDITIONS" FontSize="8" FontWeight="Bold" Foreground="#93C5FD" Margin="0,0,0,4"/>
                                <TextBlock Text="• Prices in AED (Dirhams)" FontSize="8" Foreground="#BFDBFE"/>
                                <TextBlock Text="• Quote valid for 30 days" FontSize="8" Foreground="#BFDBFE"/>
                                <TextBlock Text="• 5% VAT included" FontSize="8" Foreground="#BFDBFE"/>
                            </StackPanel>

                            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Total SQM:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,3"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Invoice.TotalSQM, StringFormat=N4}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Right"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Total Qty:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,3"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Invoice.TotalQty}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Right"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Total LM:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Invoice.TotalLM, StringFormat=N4}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Right"/>

                                    <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,3,0,6"/>

                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Sub Total (AED):" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Invoice.GrandTotal, StringFormat=N2}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>

                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="VAT (5%):" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Invoice.VatAmount, StringFormat=N2}" FontSize="8" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Right"/>

                                    <Border Grid.Row="5" Grid.ColumnSpan="2" Background="{StaticResource PrimaryBlueDark}" CornerRadius="6" Padding="8,6" Margin="0,4,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="NET TOTAL (AED):" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Invoice.NetTotal, StringFormat=N2}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Footer -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Thank you for your business!" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                            <TextBlock Text="PROGLASS AUTOMATION | Dubai, UAE" FontSize="8" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="8" Padding="8,5" Margin="0,6,0,0" BorderBrush="{StaticResource BorderLight}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="File:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0"/>
                        <TextBlock Text="{Binding CurrentFileName}" FontSize="8" FontWeight="SemiBold"/>
                        <TextBlock Text=" *" FontSize="8" FontWeight="Bold" Foreground="{StaticResource WarningRed}" Margin="2,0,0,0" Visibility="{Binding Invoice.IsDirty, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="(Unsaved)" FontSize="8" Foreground="#F59E0B" Margin="2,0,0,0" Visibility="{Binding Invoice.IsDirty, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0">
                        <TextBlock Text="LM:" FontSize="8" Foreground="{StaticResource TextSecondary}" Margin="0,0,3,0"/>
                        <TextBlock Text="{Binding IsLMToggleText}" FontSize="8" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}"/>
                    </StackPanel>
                    <TextBlock Grid.Column="2" Text="{Binding StatusMessage}" FontSize="8" Foreground="{StaticResource TextSecondary}" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Border>
        </Grid>

        <!-- ==================== CENTER - SPECS & PRICE GENERATOR ==================== -->
        <Border Grid.Column="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="8">

                    <!-- Module Selection Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#3B82F6" BorderThickness="1.5">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#DBEAFE" Offset="0"/>
                                <GradientStop Color="#BFDBFE" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#1E40AF" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🧮" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="SELECT MODULE" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0" Command="{Binding SelectSGUCommand}" Cursor="Hand" Padding="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="White" CornerRadius="8" Padding="8,6" BorderThickness="1.5" BorderBrush="#E2E8F0">
                                                <StackPanel HorizontalAlignment="Center">
                                                    <TextBlock Text="SGU" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1E40AF"/>
                                                    <TextBlock Text="Single" FontSize="7" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <Button Grid.Column="2" Command="{Binding SelectDGUCommand}" Cursor="Hand" Padding="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="White" CornerRadius="8" Padding="8,6" BorderThickness="1.5" BorderBrush="#E2E8F0">
                                                <StackPanel HorizontalAlignment="Center">
                                                    <TextBlock Text="DGU" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1E40AF"/>
                                                    <TextBlock Text="Double" FontSize="7" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <Button Grid.Column="4" Command="{Binding SelectLAMCommand}" Cursor="Hand" Padding="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="White" CornerRadius="8" Padding="8,6" BorderThickness="1.5" BorderBrush="#E2E8F0">
                                                <StackPanel HorizontalAlignment="Center">
                                                    <TextBlock Text="LAM" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1E40AF"/>
                                                    <TextBlock Text="Laminate" FontSize="7" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- SGU Input Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#3B82F6" BorderThickness="1.5"
                            Visibility="{Binding IsSGUSelected, Converter={StaticResource BoolToVis}}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#DBEAFE" Offset="0"/>
                                <GradientStop Color="#BFDBFE" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#1E40AF" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📦" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="SGU CONFIGURATION" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="8" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="THICKNESS" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                        <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding SelectedThickness}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="8" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="COLOR" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                        <ComboBox IsEditable="True"
                                                  ItemsSource="{Binding ColorHistory}"
                                                  Text="{Binding SelectedColor, UpdateSourceTrigger=PropertyChanged}"
                                                  Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"
                                                  StaysOpenOnEdit="True"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="4" Background="White" CornerRadius="8" Padding="8" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="WORK TYPE" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="3"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <RadioButton Grid.Column="0" 
                                                         IsChecked="{Binding IsAnnealedSelected}"
                                                         Cursor="Hand">
                                                <RadioButton.Template>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <Border x:Name="border" Background="#F8FAFC" CornerRadius="6" Padding="6,4" BorderThickness="1" BorderBrush="#E2E8F0">
                                                            <TextBlock Text="Annealed" FontSize="8" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#64748B"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#10B981"/>
                                                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </RadioButton.Template>
                                            </RadioButton>

                                            <RadioButton Grid.Column="2" 
                                                         IsChecked="{Binding IsFTSelected}"
                                                         Cursor="Hand">
                                                <RadioButton.Template>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <Border x:Name="border" Background="#F8FAFC" CornerRadius="6" Padding="6,4" BorderThickness="1" BorderBrush="#E2E8F0">
                                                            <TextBlock Text="FT Glass" FontSize="8" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#64748B"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#10B981"/>
                                                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </RadioButton.Template>
                                            </RadioButton>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#93C5FD">
                                <StackPanel>
                                    <TextBlock Text="SHEET PRICE (AED)" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding SGUSheetPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="8,6" FontSize="12" FontWeight="Bold" TextAlignment="Center" Foreground="#1E40AF"/>
                                </StackPanel>
                            </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="CUTTING" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding SGUCutting, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="TEMPERING" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding SGUTempering, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="4" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="WASTE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding SGUWasteFactor, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="6" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#93C5FD">
                                    <StackPanel>
                                        <TextBlock Text="PROFIT %" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding SGUProfitPercent, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- DGU Input Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#10B981" BorderThickness="1.5"
                            Visibility="{Binding IsDGUSelected, Converter={StaticResource BoolToVis}}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#D1FAE5" Offset="0"/>
                                <GradientStop Color="#A7F3D0" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#059669" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🪟" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="DGU CONFIGURATION" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <!-- Outer Glass -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#6EE7B7">
                                <StackPanel>
                                    <TextBlock Text="OUTER GLASS" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="THICKNESS" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding DGUOuterThickness}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="COLOR" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox IsEditable="True"
                                                      ItemsSource="{Binding ColorHistory}"
                                                      Text="{Binding DGUOuterColor, UpdateSourceTrigger=PropertyChanged}"
                                                      Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"
                                                      StaysOpenOnEdit="True"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="PRICE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding DGUOuterPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Air Space -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#6EE7B7">
                                <StackPanel>
                                    <TextBlock Text="AIR SPACE" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="SPACER" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9">
                                                <ComboBoxItem Content="6mm" IsSelected="True"/>
                                                <ComboBoxItem Content="8mm"/>
                                                <ComboBoxItem Content="10mm"/>
                                                <ComboBoxItem Content="12mm"/>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="ASP (AED)" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding DGUASPPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Inner Glass -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#6EE7B7">
                                <StackPanel>
                                    <TextBlock Text="INNER GLASS" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="THICKNESS" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding DGUInnerThickness}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="COLOR" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox IsEditable="True"
                                                      ItemsSource="{Binding ColorHistory}"
                                                      Text="{Binding DGUInnerColor, UpdateSourceTrigger=PropertyChanged}"
                                                      Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"
                                                      StaysOpenOnEdit="True"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="PRICE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding DGUInnerPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Waste & Profit -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#6EE7B7">
                                    <StackPanel>
                                        <TextBlock Text="WASTE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding DGUWasteFactor, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#6EE7B7">
                                    <StackPanel>
                                        <TextBlock Text="PROFIT %" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding DGUProfitPercent, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- LAM Input Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#8B5CF6" BorderThickness="1.5"
                            Visibility="{Binding IsLAMSelected, Converter={StaticResource BoolToVis}}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#EDE9FE" Offset="0"/>
                                <GradientStop Color="#DDD6FE" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#7C3AED" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔒" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="LAM CONFIGURATION" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <!-- Outer Glass -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#C4B5FD">
                                <StackPanel>
                                    <TextBlock Text="OUTER GLASS" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="THICKNESS" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding LAMOuterThickness}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="COLOR" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox IsEditable="True"
                                                      ItemsSource="{Binding ColorHistory}"
                                                      Text="{Binding LAMOuterColor, UpdateSourceTrigger=PropertyChanged}"
                                                      Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"
                                                      StaysOpenOnEdit="True"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="PRICE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding LAMOuterPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- PVB Layer -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#C4B5FD">
                                <StackPanel>
                                    <TextBlock Text="PVB LAYER" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="PVB THICKNESS" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9">
                                                <ComboBoxItem Content="0.76mm" IsSelected="True"/>
                                                <ComboBoxItem Content="1.52mm"/>
                                                <ComboBoxItem Content="2.28mm"/>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="PVB COLOR" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9">
                                                <ComboBoxItem Content="Clear" IsSelected="True"/>
                                                <ComboBoxItem Content="White"/>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="PVB PRICE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding LAMPVBPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Inner Glass -->
                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#C4B5FD">
                                <StackPanel>
                                    <TextBlock Text="INNER GLASS" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="THICKNESS" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding LAMInnerThickness}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="COLOR" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <ComboBox IsEditable="True"
                                                      ItemsSource="{Binding ColorHistory}"
                                                      Text="{Binding LAMInnerColor, UpdateSourceTrigger=PropertyChanged}"
                                                      Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9"
                                                      StaysOpenOnEdit="True"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="PRICE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding LAMInnerPrice, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6,4" FontSize="9" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Cutting & Tempering -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#C4B5FD">
                                    <StackPanel>
                                        <TextBlock Text="CUTTING" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding LAMCutting, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#C4B5FD">
                                    <StackPanel>
                                        <TextBlock Text="TEMPERING" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding LAMTempering, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Waste & Profit -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#C4B5FD">
                                    <StackPanel>
                                        <TextBlock Text="WASTE" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding LAMWasteFactor, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="#C4B5FD">
                                    <StackPanel>
                                        <TextBlock Text="PROFIT %" FontSize="6" Foreground="#94A3B8" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding LAMProfitPercent, UpdateSourceTrigger=PropertyChanged}" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="4,3" FontSize="9" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Generated Output Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#10B981" BorderThickness="1.5">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#D1FAE5" Offset="0"/>
                                <GradientStop Color="#A7F3D0" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#059669" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✨" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="CALCULATED RESULT" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#6EE7B7">
                                <StackPanel>
                                    <TextBlock Text="DESCRIPTION" FontSize="7" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding GeneratedDescription, Mode=OneWay}" IsReadOnly="True" Background="#F0FDF4" BorderBrush="#6EE7B7" BorderThickness="1" Padding="8,6" FontSize="9" FontWeight="SemiBold" Foreground="#166534" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border Background="#059669" CornerRadius="8" Padding="12,10" Margin="0,0,0,6">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="CALCULATED PRICE" FontSize="7" FontWeight="SemiBold" Foreground="#A7F3D0" HorizontalAlignment="Center" Margin="0,0,0,2"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="AED" FontSize="10" FontWeight="Bold" Foreground="White" VerticalAlignment="Bottom" Margin="0,0,4,1"/>
                                        <TextBlock Text="{Binding CalculatedPrice, StringFormat=N2}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Background="White" CornerRadius="8" Padding="8,6" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#6EE7B7">
                                <TextBlock Text="{Binding PriceCalculationSummary}" FontSize="7" Foreground="#64748B" FontFamily="Consolas" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                            </Border>

                            <Button Command="{Binding CalculatePriceCommand}" Cursor="Hand" Padding="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="#10B981" CornerRadius="8" Padding="10,8">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="🧮" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <TextBlock Text="GENERATE PRICE" FontSize="10" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#059669"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Target Specification Card -->
                    <Border CornerRadius="10" Padding="10" Margin="0,0,0,6"
                            BorderBrush="#8B5CF6" BorderThickness="1.5">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#EDE9FE" Offset="0"/>
                                <GradientStop Color="#DDD6FE" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Border Background="#7C3AED" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🎯" FontSize="10" Margin="0,0,4,0"/>
                                    <TextBlock Text="TARGET SPECIFICATION" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <Border Background="White" CornerRadius="8" Padding="8" Margin="0,0,0,6" BorderThickness="1" BorderBrush="#C4B5FD">
                                <StackPanel>
                                    <TextBlock Text="Select spec to apply price" FontSize="7" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <ComboBox ItemsSource="{Binding Invoice.Specifications}" SelectedItem="{Binding SelectedTargetSpecification}" DisplayMemberPath="SpecificationName" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Padding="8,6" FontSize="9"/>
                                </StackPanel>
                            </Border>

                            <Button Command="{Binding IncludeInSpecificationCommand}" Cursor="Hand" Padding="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="#8B5CF6" CornerRadius="8" Padding="10,8">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="✅" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <TextBlock Text="INCLUDE IN SPEC" FontSize="10" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#7C3AED"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ==================== RIGHT - FILE MANAGEMENT ==================== -->
        <Border Grid.Column="2" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderLight}" BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- File Management Card -->
                <Border Grid.Row="0" Background="White" CornerRadius="0" Padding="10" BorderBrush="{StaticResource BorderLight}" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Border Background="{StaticResource PrimaryBlueLight}" CornerRadius="6" Padding="5,3" Margin="0,0,6,0">
                                <TextBlock Text="📁" FontSize="11"/>
                            </Border>
                            <TextBlock Text="FILE MANAGEMENT" FontSize="9" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueDark}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Style="{StaticResource PrimaryButtonStyle}" Content="SAVE" Command="{Binding SaveInvoiceCommand}" Padding="6,6"/>
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButtonStyle}" Content="OPEN" Command="{Binding OpenInvoiceCommand}" Padding="6,6"/>
                        </Grid>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Style="{StaticResource SecondaryButtonStyle}" Content="NEW" Command="{Binding NewInvoiceCommand}" Padding="6,6"/>
                            <Button Grid.Column="2" Style="{StaticResource DangerButtonStyle}" Content="DELETE" Padding="6,6"/>
                        </Grid>

                        <Separator Margin="0,6"/>

                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Style="{StaticResource SecondaryButtonStyle}" Content="EXPORT" Command="{Binding ExportCsvCommand}" Padding="6,6"/>
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButtonStyle}" Content="IMPORT" Command="{Binding ImportCsvCommand}" Padding="6,6"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Recent Invoices -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource PrimaryBlueDark}" Padding="10,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🕐" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="RECENT INVOICES" FontSize="9" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <ListBox Grid.Row="1" ItemsSource="{Binding SavedFiles}" Background="White" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource BorderLight}" BorderThickness="0,0,0,1" Padding="8,6" Cursor="Hand">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <TextBlock Text="{Binding InvoiceNo}" FontSize="9" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlueDark}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding CustomerName}" FontSize="8" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding DateDisplay}" FontSize="7" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding SavedFiles.Count, Converter={StaticResource InverseBoolToVis}, FallbackValue=Visible}">
                        <TextBlock Text="📋" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="No saved invoices" FontSize="8" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

    </Grid>
</UserControl>