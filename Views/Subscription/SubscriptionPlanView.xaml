<UserControl x:Class="ProGlassAutomation.Views.Subscription.SubscriptionPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFFFFF"
             UseLayoutRounding="True" SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display">

    <UserControl.Resources>
        <!-- Sharp Enterprise Palette -->
        <SolidColorBrush x:Key="DeepBlue" Color="#1E3A8A"/>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1E40AF"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="TextDark" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#059669"/>
        <SolidColorBrush x:Key="DangerRed" Color="#DC2626"/>
        <SolidColorBrush x:Key="WarningGold" Color="#D97706"/>

        <DropShadowEffect x:Key="SharpShadow" BlurRadius="12" ShadowDepth="1" Opacity="0.1" Color="Black"/>

        <!-- Uniform Plan Card Style -->
        <Style x:Key="PlanCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="22"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource SharpShadow}"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="35">
            <!-- 1. HEADER SECTION -->
            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                <TextBlock Text="ENTERPRISE LICENSING" FontSize="32" FontWeight="Black" Foreground="{StaticResource DeepBlue}" HorizontalAlignment="Center"/>
                <TextBlock Text="Manage production tiers and system activation" FontSize="15" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0" FontWeight="Bold"/>
            </StackPanel>

            <!-- 2. STATUS STRIP (Names synced with UpdateStatus logic) -->
            <Border Background="#F8FAFC" CornerRadius="15" Padding="25" Margin="0,0,0,30" BorderBrush="#E2E8F0" BorderThickness="1">
                <UniformGrid Columns="4">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="STATUS" FontSize="11" FontWeight="Black" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" Text="INACTIVE" FontSize="22" FontWeight="Black" Foreground="{StaticResource DangerRed}" Margin="0,8,0,0"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="DAYS LEFT" FontSize="11" FontWeight="Black" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="DaysText" Text="0" FontSize="22" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,8,0,0"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="ACTIVE NODES" FontSize="11" FontWeight="Black" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PCsText" Text="0/0" FontSize="22" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,8,0,0"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="PRODUCTION TIER" FontSize="11" FontWeight="Black" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PlanTypeText" Text="NONE" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryBlue}" Margin="0,8,0,0"/>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <!-- 3. ACTIVATION CONSOLE (Synced with logic) -->
            <Border Background="White" CornerRadius="18" Padding="35" Margin="0,0,0,40" BorderBrush="{StaticResource DeepBlue}" BorderThickness="2" Effect="{StaticResource SharpShadow}">
                <StackPanel>
                    <TextBlock Text="INPUT ENGINE ACTIVATION KEY" FontSize="16" FontWeight="Black" Foreground="{StaticResource DeepBlue}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock x:Name="SelectedPlanText" Text="Select a production tier below to begin" FontSize="13" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" Margin="0,0,0,15" FontWeight="Bold"/>

                    <TextBox x:Name="LicenseKeyInput" Height="65" FontSize="26" FontWeight="Black" Padding="20,0" VerticalContentAlignment="Center" TextAlignment="Center" CharacterCasing="Upper" TextChanged="LicenseKeyInput_TextChanged" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="2"/>

                    <Grid Margin="0,25,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="ACTIVATE ENGINE" Click="ActivateKey_Click" Background="{StaticResource SuccessGreen}" Foreground="White" FontWeight="Black" Height="55" Margin="0,0,10,0" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="12"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Grid.Column="1" Content="DEACTIVATE SYSTEM" Click="DeactivateKey_Click" Background="{StaticResource DangerRed}" Foreground="White" FontWeight="Black" Height="55" Margin="10,0,0,0" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="12"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                    <TextBlock x:Name="ErrorText" Text="" FontSize="12" Foreground="{StaticResource DangerRed}" HorizontalAlignment="Center" Margin="0,15,0,0" FontWeight="Black"/>
                </StackPanel>
            </Border>

            <!-- 4. PLAN MATRIX (Borders & Tags Strictly Synced with UpdatePlanSelection) -->
            <TextBlock Text="WEEKLY PRODUCTION PLANS" FontSize="14" FontWeight="Black" Foreground="{StaticResource WarningGold}" Margin="5,0,0,15"/>
            <WrapPanel Margin="0,0,0,30">
                <Border x:Name="WK1Border" Tag="WK1" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource WarningGold}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="1 PC" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 100" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="WK1Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="WK3Border" Tag="WK3" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource WarningGold}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="3 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 150" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="WK3Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="WK5Border" Tag="WK5" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource WarningGold}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="5 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 200" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="WK5Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <TextBlock Text="MONTHLY PRODUCTION PLANS" FontSize="14" FontWeight="Black" Foreground="{StaticResource PrimaryBlue}" Margin="5,0,0,15"/>
            <WrapPanel Margin="0,0,0,30">
                <Border x:Name="MT1Border" Tag="MT1" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource PrimaryBlue}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="1 PC" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 250" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="MT1Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="MT3Border" Tag="MT3" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource PrimaryBlue}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="3 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 300" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="MT3Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="MT5Border" Tag="MT5" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource PrimaryBlue}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="5 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 400" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="MT5Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- BI-ANNUAL (Synced names S61Border... but Tags 6M1...) -->
            <TextBlock Text="BI-ANNUAL PRODUCTION PLANS" FontSize="14" FontWeight="Black" Foreground="#7C3AED" Margin="5,0,0,15"/>
            <WrapPanel Margin="0,0,0,30">
                <Border x:Name="S61Border" Tag="6M1" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="#7C3AED" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="1 PC" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 1200" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="S61Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="S63Border" Tag="6M3" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="#7C3AED" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="3 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 1500" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="S63Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="S65Border" Tag="6M5" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="#7C3AED" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="5 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 2000" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="S65Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- ANNUAL -->
            <TextBlock Text="ANNUAL PRODUCTION PLANS" FontSize="14" FontWeight="Black" Foreground="{StaticResource SuccessGreen}" Margin="5,0,0,15"/>
            <WrapPanel Margin="0,0,0,30">
                <Border x:Name="YR1Border" Tag="YR1" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource SuccessGreen}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="1 PC" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 2000" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="YR1Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="YR3Border" Tag="YR3" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource SuccessGreen}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="3 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 2400" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="YR3Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border x:Name="YR5Border" Tag="YR5" Style="{StaticResource PlanCardStyle}" MouseLeftButtonDown="Plan_Click">
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="{StaticResource SuccessGreen}" CornerRadius="6" Padding="12,4" HorizontalAlignment="Left">
                            <TextBlock Text="5 PCs" FontWeight="Black" Foreground="White" FontSize="11"/>
                        </Border>
                        <TextBlock Text="AED 3000" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,15,0,0"/>
                        <TextBlock x:Name="YR5Check" Text="Select" FontWeight="Black" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- CONTACT SALES -->
            <Border Background="{StaticResource DeepBlue}" CornerRadius="18" Padding="40" Margin="0,20,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="REQUEST ENTERPRISE LICENSE KEY" FontSize="22" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Contact our activation team for secure bank transfer and key generation" Foreground="#93C5FD" Margin="0,5,0,30" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="WHATSAPP ACTIVATION" Click="ContactWhatsApp_Click" Background="White" Foreground="{StaticResource DeepBlue}" FontWeight="Black" Padding="35,14" Margin="0,0,15,0" BorderThickness="0" Cursor="Hand"/>
                        <Button Content="EMAIL SUPPORT" Click="ContactEmail_Click" Background="Transparent" Foreground="White" BorderBrush="White" BorderThickness="2" FontWeight="Black" Padding="35,14" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>