<UserControl x:Class="ProGlassAutomation.Views.DGU.DguView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ProGlassAutomation.ViewModels">

    <UserControl.DataContext>
        <vm:DguViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <FontFamily x:Key="PrimaryFont">Segoe UI, Inter, sans-serif</FontFamily>

        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <Style x:Key="GlassCardBlue" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#EFF6FF" Offset="0"/>
                        <GradientStop Color="#DBEAFE" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassCardGreen" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#F0FDF4" Offset="0"/>
                        <GradientStop Color="#DCFCE7" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassCardOrange" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFBEB" Offset="0"/>
                        <GradientStop Color="#FEF3C7" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassCardRed" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FEF2F2" Offset="0"/>
                        <GradientStop Color="#FEE2E2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassCardPurple" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FAF5FF" Offset="0"/>
                        <GradientStop Color="#F3E8FF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="IsEditable" Value="True"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="MaxDropDownHeight" Value="200"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#3B82F6" Offset="0"/>
                                    <GradientStop Color="#2563EB" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSuccess" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#EF4444" Offset="0"/>
                                    <GradientStop Color="#DC2626" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSmallDelete" TargetType="Button">
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#F1F5F9">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Style="{StaticResource GlassCard}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="DGU Calculator" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="Double Glazed Unit Price Calculator" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1" CornerRadius="16" Padding="20,12">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0891B2" Offset="0"/>
                                <GradientStop Color="#06B6D4" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="UNIT PRICE" FontSize="11" FontWeight="Medium" Foreground="#E0F2FE" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBlock Text="{Binding Result, StringFormat={}{0:F2}}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="AED" FontSize="13" Foreground="#E0F2FE" VerticalAlignment="Bottom" Margin="8,0,0,4"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- MAIN CONTENT -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: Outer & Inner Glass -->
                <StackPanel Grid.Column="0" Margin="0,0,8,0">

                    <!-- Outer Glass -->
                    <Border Style="{StaticResource GlassCardBlue}" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#3B82F6"/>
                                <TextBlock Grid.Column="1" Text="Outer Glass" Style="{StaticResource SectionTitle}" Foreground="#1E40AF" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding CategoryOptions}" SelectedValue="{Binding OuterCategory}"/>

                            <TextBlock Text="Thickness" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedValue="{Binding OuterThickness}"/>

                            <TextBlock Text="Color" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <ComboBox ItemsSource="{Binding ColorOptions}" SelectedValue="{Binding OuterColor}" DisplayMemberPath="Name" SelectedValuePath="Name"/>

                            <TextBlock Text="Sheet Price (AED)" Style="{StaticResource FieldLabel}" Margin="0,12,0,0"/>
                            <TextBox Text="{Binding OuterPrice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Inner Glass -->
                    <Border Style="{StaticResource GlassCardGreen}">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#22C55E"/>
                                <TextBlock Grid.Column="1" Text="Inner Glass" Style="{StaticResource SectionTitle}" Foreground="#166534" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding CategoryOptions}" SelectedValue="{Binding InnerCategory}"/>

                            <TextBlock Text="Thickness" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedValue="{Binding InnerThickness}"/>

                            <TextBlock Text="Color" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <ComboBox ItemsSource="{Binding ColorOptions}" SelectedValue="{Binding InnerColor}" DisplayMemberPath="Name" SelectedValuePath="Name"/>

                            <TextBlock Text="Sheet Price (AED)" Style="{StaticResource FieldLabel}" Margin="0,12,0,0"/>
                            <TextBox Text="{Binding InnerPrice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- MIDDLE: Spacer & Wastage -->
                <StackPanel Grid.Column="1" Margin="8,0">

                    <!-- Spacer Settings -->
                    <Border Style="{StaticResource GlassCardOrange}" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#F59E0B"/>
                                <TextBlock Grid.Column="1" Text="Spacer Settings" Style="{StaticResource SectionTitle}" Foreground="#B45309" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Spacer Thickness" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding SpacerOptions}" SelectedValue="{Binding Spacer}"/>

                            <TextBlock Text="Spacer Charge (AED)" Style="{StaticResource FieldLabel}" Margin="0,12,0,0"/>
                            <TextBox Text="{Binding SpacerCharge, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                            <TextBlock Text="Sealant Charge (AED)" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding SealantCharge, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                            <TextBlock Text="Gas Charge (AED)" Style="{StaticResource FieldLabel}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding GasCharge, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Wastage -->
                    <Border Style="{StaticResource GlassCardRed}">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#EF4444"/>
                                <TextBlock Grid.Column="1" Text="Wastage Factor" Style="{StaticResource SectionTitle}" Foreground="#DC2626" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Wastage %" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding WastageOptions}" SelectedValue="{Binding Wastage}"/>

                            <Border Background="White" CornerRadius="10" Padding="12" Margin="0,12,0,0" BorderBrush="#FECACA" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Wastage Factor" FontSize="10" Foreground="#991B1B"/>
                                    <TextBlock Text="{Binding WastageFactorDisplay}" FontSize="22" FontWeight="Bold" Foreground="#DC2626"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- RIGHT: Profit, Actions, Summary -->
                <StackPanel Grid.Column="2" Margin="8,0,0,0">

                    <!-- Profit Margin -->
                    <Border Style="{StaticResource GlassCardPurple}" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#8B5CF6"/>
                                <TextBlock Grid.Column="1" Text="Profit Margin" Style="{StaticResource SectionTitle}" Foreground="#5B21B6" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Select Profit %" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding ProfitMarginOptions}" SelectedValue="{Binding ProfitMargin}"/>

                            <Border Background="White" CornerRadius="10" Padding="12" Margin="0,12,0,0" BorderBrush="#DDD6FE" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Profit Added" FontSize="10" Foreground="#5B21B6"/>
                                    <TextBlock Text="{Binding ProfitMarginDisplay}" FontSize="22" FontWeight="Bold" Foreground="#7C3AED"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Actions -->
                    <Border Style="{StaticResource GlassCard}" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#64748B"/>
                                <TextBlock Grid.Column="1" Text="Actions" Style="{StaticResource SectionTitle}" Foreground="#334155" Margin="10,0,0,0"/>
                            </Grid>

                            <Button Content="Save DGU" Command="{Binding SaveCommand}" Style="{StaticResource BtnPrimary}" Margin="0,0,0,6"/>
                            <Button Content="Export PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource BtnSuccess}" Margin="0,0,0,6"/>
                            <Button Content="Clear" Command="{Binding ClearCommand}" Style="{StaticResource BtnDanger}"/>
                        </StackPanel>
                    </Border>

                    <!-- Price Summary -->
                    <Border Style="{StaticResource GlassCard}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#ECFDF5" Offset="0"/>
                                <GradientStop Color="#D1FAE5" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#10B981"/>
                                <TextBlock Grid.Column="1" Text="Price Summary" Style="{StaticResource SectionTitle}" Foreground="#065F46" Margin="10,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Glass Cost" FontSize="12" Foreground="#047857"/>
                                <TextBlock Grid.Column="1" Text="{Binding TotalGlassCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="SemiBold" Foreground="#059669" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Processing Cost" FontSize="12" Foreground="#047857"/>
                                <TextBlock Grid.Column="1" Text="{Binding ProcessingCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="SemiBold" Foreground="#059669" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Base Cost" FontSize="12" Foreground="#047857"/>
                                <TextBlock Grid.Column="1" Text="{Binding BaseCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="SemiBold" Foreground="#059669" HorizontalAlignment="Right"/>
                            </Grid>

                            <Border Height="1" Background="#A7F3D0" Margin="0,10"/>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="UNIT PRICE" FontSize="13" FontWeight="Bold" Foreground="#065F46"/>
                                <TextBlock Grid.Column="1" Text="{Binding Result, StringFormat={}{0:F2} AED}" FontSize="14" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="VAT (5%)" FontSize="12" Foreground="#047857"/>
                                <TextBlock Grid.Column="1" Text="{Binding VatAmount, StringFormat={}{0:F2} AED}" FontSize="12" Foreground="#059669" HorizontalAlignment="Right"/>
                            </Grid>

                            <Border Height="1" Background="#A7F3D0" Margin="0,6,0,8"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="GRAND TOTAL" FontSize="14" FontWeight="Bold" Foreground="#065F46"/>
                                <TextBlock Grid.Column="1" Text="{Binding GrossTotal, StringFormat={}{0:F2} AED}" FontSize="16" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- HISTORY SECTION -->
            <Border Grid.Row="2" Style="{StaticResource GlassCard}" Margin="0,12,0,0">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Calculation History" Style="{StaticResource SectionTitle}" Margin="0"/>
                        <Button Grid.Column="1" Content="Clear All" Command="{Binding ClearAllCommand}" Style="{StaticResource BtnDanger}" Padding="12,6"/>
                    </Grid>

                    <Border Background="#F8FAFC" CornerRadius="12" MinHeight="80" MaxHeight="220"
                            Visibility="{Binding IsHistoryVisible, Converter={StaticResource BoolToVis}}"
                            BorderBrush="#E2E8F0" BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Records}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="12" Padding="14" Margin="4" MinWidth="260"
                                                BorderBrush="#E2E8F0" BorderThickness="1">
                                            <StackPanel>
                                                <Grid Margin="0,0,0,8">
                                                    <Border Background="#F1F5F9" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left">
                                                        <TextBlock Text="{Binding CreatedAt}" FontSize="10" Foreground="#64748B"/>
                                                    </Border>
                                                    <Button Content="X" 
                                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                            CommandParameter="{Binding}" 
                                                            Style="{StaticResource BtnSmallDelete}"
                                                            HorizontalAlignment="Right"/>
                                                </Grid>

                                                <TextBlock Text="{Binding DisplayText}" FontSize="11" Foreground="#334155" TextWrapping="Wrap" Margin="0,0,0,6"/>

                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                    <Border Background="#FEF2F2" CornerRadius="4" Padding="6,2" Margin="0,0,4,0">
                                                        <TextBlock FontSize="10" Foreground="#DC2626">
                                                            <Run Text="Waste: "/><Run Text="{Binding Wastage}"/>
                                                        </TextBlock>
                                                    </Border>
                                                    <Border Background="#F3E8FF" CornerRadius="4" Padding="6,2">
                                                        <TextBlock FontSize="10" Foreground="#7C3AED">
                                                            <Run Text="Profit: "/><Run Text="{Binding ProfitMargin}"/>
                                                        </TextBlock>
                                                    </Border>
                                                </StackPanel>

                                                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#10B981">
                                                    <Run Text="{Binding Result, StringFormat={}{0:F2}}"/><Run Text=" AED"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <!-- Empty State -->
                    <Border Background="#F8FAFC" CornerRadius="12" Padding="40" HorizontalAlignment="Center"
                            BorderBrush="#E2E8F0" BorderThickness="1">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="No calculations saved yet" FontSize="13" Foreground="#64748B" HorizontalAlignment="Center"/>
                            <TextBlock Text="Save a calculation to see it here" FontSize="11" Foreground="#94A3B8" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>