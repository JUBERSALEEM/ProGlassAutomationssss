<UserControl x:Class="ProGlassAutomation.Views.SGU.SguView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ProGlassAutomation.Views.SGU"
             Background="#F1F5F9">
    <UserControl.DataContext>
        <vm:SguViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="T" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E3A5F"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="L" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,6,0,3"/>
        </Style>
        <Style x:Key="I" TargetType="TextBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style x:Key="C" TargetType="ComboBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="IsEditable" Value="True"/>
        </Style>
        <Style x:Key="B" TargetType="Button">
            <Setter Property="Background" Value="#0A6ED1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BG" TargetType="Button" BasedOn="{StaticResource B}">
            <Setter Property="Background" Value="#059669"/>
        </Style>
        <Style x:Key="BR" TargetType="Button" BasedOn="{StaticResource B}">
            <Setter Property="Background" Value="#DC2626"/>
        </Style>
        <Style x:Key="N" TargetType="Border">
            <Setter Property="Background" Value="#0A6ED1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Border Background="#0A6ED1" CornerRadius="8" Padding="10,6" Margin="0,0,12,0">
                        <TextBlock Text="📊" FontSize="18"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="SGU CALCULATOR" FontSize="18" FontWeight="Bold" Foreground="#1E3A5F"/>
                        <TextBlock Text="Single Glazed Unit Pricing" FontSize="10" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
                <Border Background="#ECFDF5" CornerRadius="10" Padding="16,10" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Unit Price:" FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Result, StringFormat='{}{0:F2}'}" FontSize="26" FontWeight="Bold" Foreground="#059669"/>
                        <TextBlock Text=" AED" FontSize="12" Foreground="#94A3B8" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                <Border Style="{StaticResource Card}" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="🪟 GLASS SPECIFICATION" Style="{StaticResource T}"/>
                        <TextBlock Text="Thickness" Style="{StaticResource L}"/>
                        <ComboBox ItemsSource="{Binding ThicknessOptions}" SelectedItem="{Binding Thickness}" Style="{StaticResource C}"/>
                        <TextBlock Text="Color" Style="{StaticResource L}"/>
                        <ComboBox ItemsSource="{Binding ColorOptions}" SelectedItem="{Binding Color}" Style="{StaticResource C}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="💰 PRICE BREAKUP" Style="{StaticResource T}"/>
                        <TextBlock Text="Sheet Price (AED)" Style="{StaticResource L}"/>
                        <TextBox Text="{Binding Sheet, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource I}"/>
                        <TextBlock Text="Cutting Charge (AED)" Style="{StaticResource L}"/>
                        <TextBox Text="{Binding Cutting, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource I}"/>
                        <TextBlock Text="Tempering Charge (AED)" Style="{StaticResource L}"/>
                        <TextBox Text="{Binding Tempering, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource I}"/>
                        <TextBlock Text="Profit Margin" Style="{StaticResource L}"/>
                        <ComboBox ItemsSource="{Binding ProfitOptions}" SelectedItem="{Binding Profit}" Style="{StaticResource C}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="6,0">
                <Border Style="{StaticResource Card}" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="📖 HOW TO USE" Style="{StaticResource T}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Style="{StaticResource N}">
                                <TextBlock Text="1" Foreground="White" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Select thickness &amp; color" Foreground="#475569" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Style="{StaticResource N}">
                                <TextBlock Text="2" Foreground="White" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Enter prices" Foreground="#475569" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Style="{StaticResource N}">
                                <TextBlock Text="3" Foreground="White" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Set profit margin" Foreground="#475569" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Style="{StaticResource N}">
                                <TextBlock Text="4" Foreground="White" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Unit price auto-calc" Foreground="#475569" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Style="{StaticResource N}">
                                <TextBlock Text="5" Foreground="White" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Save or Export PDF" Foreground="#475569" FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="⚡ ACTIONS" Style="{StaticResource T}"/>
                        <Button Content="💾 Save" Command="{Binding SaveCommand}" Style="{StaticResource B}"/>
                        <Button Content="📄 Export PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource BG}"/>
                        <Button Content="🗑️ Clear" Command="{Binding ClearCommand}" Style="{StaticResource BR}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            <Grid Grid.Column="2" Margin="6,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="🧮 CALCULATION FORMULA" Style="{StaticResource T}"/>
                        <Border Background="#F0F9FF" CornerRadius="8" Padding="10">
                            <StackPanel>
                                <TextBlock Text="FORMULA BREAKDOWN" FontSize="10" FontWeight="Bold" Foreground="#0A6ED1" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="25"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="1." Foreground="#64748B" FontSize="11"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Base Sheet / 0.85" Foreground="#475569" FontSize="11"/>
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="= Base Price" Foreground="#1E3A5F" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="2." Foreground="#64748B" FontSize="11" Margin="0,6,0,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="+ Cutting + Tempering" Foreground="#475569" FontSize="11" Margin="0,6,0,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="= Total Cost" Foreground="#1E3A5F" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="3." Foreground="#64748B" FontSize="11" Margin="0,6,0,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="x (1 + Profit%)" Foreground="#475569" FontSize="11" Margin="0,6,0,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="2" Text="= Unit Price" Foreground="#059669" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
                <Border Grid.Row="1" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="💡 LIVE EXAMPLE" Style="{StaticResource T}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="1." Foreground="#64748B" FontSize="11"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Sheet, StringFormat='Sheet: {0:F2} AED'}" Foreground="#475569" FontSize="11"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding BasePrice, StringFormat='/{0:F2}'}" Foreground="#1E3A5F" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="2." Foreground="#64748B" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Cutting, StringFormat='+ Cutting: {0:F2} AED'}" Foreground="#475569" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding AfterCutting, StringFormat='+{0:F2}'}" Foreground="#1E3A5F" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="3." Foreground="#64748B" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Tempering, StringFormat='+ Tempering: {0:F2} AED'}" Foreground="#475569" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding AfterTempering, StringFormat='+{0:F2}'}" Foreground="#1E3A5F" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="4." Foreground="#64748B" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Profit, StringFormat='x Profit: {0}%'}" Foreground="#475569" FontSize="11" Margin="0,6,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding Result, StringFormat='={0:F2}'}" Foreground="#059669" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                        </Grid>
                        <Border Background="#059669" CornerRadius="8" Padding="14,10" Margin="0,12,0,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="UNIT PRICE" FontSize="10" Foreground="#A7F3D0" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Result, StringFormat='{}{0:F2} AED'}" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
            <Border Grid.Column="3" Style="{StaticResource Card}" Margin="6,0,0,0">
                <StackPanel>
                    <TextBlock Text="📜 HISTORY" Style="{StaticResource T}"/>
                    <Border Background="#F0F9FF" CornerRadius="8" Padding="6">
                        <ListBox ItemsSource="{Binding Records}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="6" Padding="10,8" Margin="0,2" BorderBrush="#E2E8F0" BorderThickness="1">
                                        <TextBlock Text="{Binding DisplayText}" FontSize="11" Foreground="#334155" TextWrapping="Wrap"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,10,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="#10B981" Margin="0,0,8,0"/>
                    <TextBlock Text="Ready" Foreground="#059669" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="#CBD5E1" Margin="8,0"/>
                    <TextBlock Text="Auto-save enabled" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Glass:" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Spec}" Foreground="#1E3A5F" FontWeight="SemiBold" Margin="6,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>