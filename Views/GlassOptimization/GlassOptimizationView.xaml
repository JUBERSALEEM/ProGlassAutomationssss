<UserControl x:Class="ProGlassAutomation.Views.GlassOptimization.GlassOptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ProGlassAutomation.Views.GlassOptimization">

    <UserControl.DataContext>
        <vm:GlassOptimizationViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- ═══════════════════════════════════════════════════════
             COLORFUL SCROLLBAR STYLES
             ═══════════════════════════════════════════════════════ -->

        <Style x:Key="ColorfulScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#A0AEC0" CornerRadius="5" Margin="2,0"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="10"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ColorfulScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1" Grid.Row="0"
                                       Style="{StaticResource ColorfulScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Column="0" Grid.Row="1"
                                       Style="{StaticResource ColorfulScrollBar}"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══════════════════════════════════════════════════════
             CARD STYLES
             ═══════════════════════════════════════════════════════ -->

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style x:Key="CardBlue" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#EFF6FF" Offset="0"/>
                        <GradientStop Color="#DBEAFE" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#BFDBFE"/>
        </Style>

        <Style x:Key="CardOrange" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFBEB" Offset="0"/>
                        <GradientStop Color="#FEF3C7" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#FDE68A"/>
        </Style>

        <Style x:Key="CardIndigo" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#EEF2FF" Offset="0"/>
                        <GradientStop Color="#E0E7FF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#C7D2FE"/>
        </Style>

        <!-- ═══════════════════════════════════════════════════════
             BUTTON STYLES
             ═══════════════════════════════════════════════════════ -->

        <Style x:Key="BtnGreen" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnRed" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteBtn" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══════════════════════════════════════════════════════
             INPUT BOX STYLE
             ═══════════════════════════════════════════════════════ -->

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C7D2FE"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#4338CA"/>
            <Setter Property="CaretBrush" Value="#4338CA"/>
            <Setter Property="AcceptsReturn" Value="False"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>

        <Style x:Key="SheetCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#F1F5F9">
        <ScrollViewer Style="{StaticResource ColorfulScrollViewer}" 
                      VerticalScrollBarVisibility="Auto" 
                      Padding="16">
            <StackPanel MaxWidth="1100">

                <!-- HEADER -->
                <Border Style="{StaticResource CardBlue}" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="10" Width="44" Height="44" Margin="0,0,10,0">
                            <TextBlock Text="🪟" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Glass Optimization" FontSize="18" FontWeight="Bold" Foreground="#1E40AF"/>
                            <TextBlock Text="Calculate optimization results for glass sheets" FontSize="11" Foreground="#3B82F6" Margin="0,2,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="+ Add" Command="{Binding AddCommand}" Style="{StaticResource BtnGreen}" Margin="0,0,6,0"/>
                            <Button Content="Clear" Command="{Binding ClearAllCommand}" Style="{StaticResource BtnRed}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- SHEETS LIST -->
                <ItemsControl ItemsSource="{Binding Sheets}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SheetCard}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Sheet Header -->
                                    <Grid Grid.Row="0" Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <Border Background="#E0E7FF" CornerRadius="6" Padding="8,4">
                                                <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Bold" Foreground="#3730A3"/>
                                            </Border>
                                            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontSize="10" Foreground="#64748B">
                                                <Run Text="{Binding Category}"/> • <Run Text="{Binding Thickness}"/> • <Run Text="{Binding Color}"/> • <Run Text="{Binding ColorType}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <Button Grid.Column="1" Content="✕" 
                                                Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}" 
                                                Style="{StaticResource DeleteBtn}"/>
                                    </Grid>

                                    <!-- Input Values & Result -->
                                    <Border Grid.Row="1" Style="{StaticResource CardIndigo}" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Price -->
                                            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                                <TextBlock Text="PRICE (AED)" Style="{StaticResource FieldLabel}" Foreground="#818CF8"/>
                                                <TextBox Style="{StaticResource InputBox}"
                                                         Text="{Binding Price, UpdateSourceTrigger=LostFocus, StringFormat={}{0:G}}"
                                                         GotFocus="InputBox_GotFocus"
                                                         PreviewMouseLeftButtonDown="InputBox_PreviewMouseLeftButtonDown"
                                                         PreviewKeyDown="InputBox_PreviewKeyDown"/>
                                            </StackPanel>

                                            <!-- Optimization -->
                                            <StackPanel Grid.Column="1" Margin="3,0">
                                                <TextBlock Text="OPTIMIZATION (%)" Style="{StaticResource FieldLabel}" Foreground="#818CF8"/>
                                                <TextBox Style="{StaticResource InputBox}"
                                                         Text="{Binding Optimization, UpdateSourceTrigger=LostFocus, StringFormat={}{0:G}}"
                                                         GotFocus="InputBox_GotFocus"
                                                         PreviewMouseLeftButtonDown="InputBox_PreviewMouseLeftButtonDown"
                                                         PreviewKeyDown="InputBox_PreviewKeyDown"/>
                                            </StackPanel>

                                            <!-- Chargeable SQM -->
                                            <StackPanel Grid.Column="2" Margin="3,0,0,0">
                                                <TextBlock Text="CHARGEABLE SQM" Style="{StaticResource FieldLabel}" Foreground="#818CF8"/>
                                                <TextBox Style="{StaticResource InputBox}"
                                                         Text="{Binding ChargeableSqm, UpdateSourceTrigger=LostFocus, StringFormat={}{0:G}}"
                                                         GotFocus="InputBox_GotFocus"
                                                         PreviewMouseLeftButtonDown="InputBox_PreviewMouseLeftButtonDown"
                                                         PreviewKeyDown="InputBox_PreviewKeyDown"/>
                                            </StackPanel>

                                            <!-- Result -->
                                            <StackPanel Grid.Column="3" Margin="10,0,0,0" VerticalAlignment="Center">
                                                <Border Background="#15803D" CornerRadius="8" Padding="14,10" BorderBrush="#22C55E" BorderThickness="2">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="RESULT" FontSize="8" FontWeight="Bold" Foreground="#BBF7D0" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="AED" FontSize="9" Foreground="#DCFCE7" HorizontalAlignment="Center" Margin="0,1,0,0"/>
                                                        <TextBlock Text="{Binding FinalResultText}" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State -->
                <Border Style="{StaticResource CardOrange}" Padding="40"
                        Visibility="{Binding Sheets.Count, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🪟" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="No sheets added yet" FontSize="14" FontWeight="Bold" Foreground="#B45309" HorizontalAlignment="Center"/>
                        <TextBlock Text="Click '+ Add' to start calculating" FontSize="11" Foreground="#D97706" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>