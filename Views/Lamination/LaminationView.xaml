<UserControl x:Class="ProGlassAutomation.Views.Lamination.LaminationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ProGlassAutomation.Views.Lamination">

    <UserControl.DataContext>
        <vm:LaminationViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Matching SguView Palette -->
        <SolidColorBrush x:Key="DeepBlue" Color="#1E3A8A"/>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#059669"/>
        <SolidColorBrush x:Key="WarningOrange" Color="#D97706"/>
        <SolidColorBrush x:Key="TextDark" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderSharp" Color="#CBD5E1"/>
        <SolidColorBrush x:Key="BgLight" Color="#F8FAFC"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>

        <!-- Matching ModuleBorderStyle -->
        <Style x:Key="ModuleBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18,15"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSharp}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <!-- Sharp Input Style -->
        <Style x:Key="SharpInput" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSharp}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="SharpCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSharp}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="IsEditable" Value="True"/>
        </Style>

        <!-- Field Label -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Section Title -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Black"/>
            <Setter Property="Foreground" Value="{StaticResource DeepBlue}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Button -->
        <Style x:Key="BtnSuccess" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Button -->
        <Style x:Key="BtnDanger" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete Button -->
        <Style x:Key="DeleteBtn" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stat Card -->
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgLight}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSharp}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#F1F5F9">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="25">
            <StackPanel>

                <!-- HEADER - Matching SguView Style -->
                <Border Background="{StaticResource DeepBlue}" CornerRadius="12" Padding="24,18" Margin="0,0,0,25">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <Border Width="55" Height="55" CornerRadius="10" Background="White">
                                <TextBlock Text="🪟" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="LAMINATION CALCULATOR" FontSize="22" FontWeight="Black" Foreground="White"/>
                                <TextBlock Text="LAMINATED GLASS UNIT (2 GLASS + PVB)" FontSize="10" Foreground="#93C5FD" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Inventory Stats -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,20,0">
                            <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding CategoryTotal}" FontSize="20" FontWeight="Black" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="CATEGORIES" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ThicknessTotal}" FontSize="20" FontWeight="Black" Foreground="{StaticResource SuccessGreen}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="THICKNESS" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ColorTotal}" FontSize="20" FontWeight="Black" Foreground="{StaticResource WarningOrange}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="COLORS" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding PVBTypeTotal}" FontSize="20" FontWeight="Black" Foreground="#EC4899" HorizontalAlignment="Center"/>
                                    <TextBlock Text="PVB TYPES" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource StatCard}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Records.Count}" FontSize="20" FontWeight="Black" Foreground="{StaticResource DeepBlue}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="HISTORY" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Price Display -->
                        <Border Grid.Column="3" Background="#111827" CornerRadius="12" Padding="22,14" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel VerticalAlignment="Center" Margin="0,0,18,0">
                                    <TextBlock Text="UNIT PRICE" FontSize="9" FontWeight="Bold" Foreground="#60A5FA"/>
                                    <TextBlock Text="{Binding Result, StringFormat={}{0:F2}}" FontSize="30" FontWeight="Black" Foreground="White"/>
                                </StackPanel>
                                <Border Width="1" Background="{StaticResource PrimaryBlue}" Margin="0,5"/>
                                <TextBlock Text="AED" FontSize="18" FontWeight="Black" Foreground="{StaticResource PrimaryBlue}" VerticalAlignment="Center" Margin="18,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- MAIN CONTENT - 3 Column Grid -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- OUTER GLASS -->
                    <Border Grid.Column="0" Style="{StaticResource ModuleBorderStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="{StaticResource PrimaryBlue}"/>
                                <TextBlock Grid.Column="1" Text="OUTER GLASS" Style="{StaticResource SectionTitle}" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding CategoryOptions}" SelectedValue="{Binding OuterCategory}" IsEditable="True"/>

                            <TextBlock Text="Thickness" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding ThicknessOptions}" SelectedValue="{Binding OuterThickness}" IsEditable="True"/>

                            <TextBlock Text="Color" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding ColorOptions}" SelectedValue="{Binding OuterColor}" DisplayMemberPath="Name" SelectedValuePath="Name" IsEditable="True"/>

                            <TextBlock Text="Sheet Price (AED)" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding OuterPrice, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>
                        </StackPanel>
                    </Border>

                    <!-- INNER GLASS -->
                    <Border Grid.Column="1" Style="{StaticResource ModuleBorderStyle}" Margin="12,0">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="{StaticResource SuccessGreen}"/>
                                <TextBlock Grid.Column="1" Text="INNER GLASS" Style="{StaticResource SectionTitle}" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding CategoryOptions}" SelectedValue="{Binding InnerCategory}" IsEditable="True"/>

                            <TextBlock Text="Thickness" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding ThicknessOptions}" SelectedValue="{Binding InnerThickness}" IsEditable="True"/>

                            <TextBlock Text="Color" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding ColorOptions}" SelectedValue="{Binding InnerColor}" DisplayMemberPath="Name" SelectedValuePath="Name" IsEditable="True"/>

                            <TextBlock Text="Sheet Price (AED)" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding InnerPrice, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>
                        </StackPanel>
                    </Border>

                    <!-- PVB LAYER & SETTINGS -->
                    <Border Grid.Column="2" Style="{StaticResource ModuleBorderStyle}" Margin="12,0,0,0">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#EC4899"/>
                                <TextBlock Grid.Column="1" Text="PVB LAYER" Style="{StaticResource SectionTitle}" Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="PVB Type" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding PVBTypes}" SelectedValue="{Binding PVBType}" IsEditable="True"/>

                            <TextBlock Text="PVB Price (AED)" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding PVBPrice, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>

                            <TextBlock Text="Lamination Type" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding LaminationTypes}" SelectedValue="{Binding LaminationType}" IsEditable="True"/>

                            <TextBlock Text="Wastage %" Style="{StaticResource FieldLabel}" Margin="0,28,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding WastageOptions}" SelectedValue="{Binding Wastage}"/>

                            <TextBlock Text="Profit Margin" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding ProfitMarginOptions}" SelectedValue="{Binding ProfitMargin}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- PROCESSING CHARGES & ACTIONS -->
                <Grid Margin="0,25,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Processing Charges -->
                    <Border Grid.Column="0" Style="{StaticResource ModuleBorderStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="PROCESSING CHARGES" Style="{StaticResource SectionTitle}"/>

                            <TextBlock Text="Cutting (AED)" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding CuttingCharge, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>

                            <TextBlock Text="Tempering (AED)" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding TemperingCharge, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>

                            <TextBlock Text="Other Charges (AED)" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding OtherCharges, UpdateSourceTrigger=LostFocus, TargetNullValue=''}"/>

                            <Border Background="{StaticResource BgLight}" CornerRadius="10" Padding="14" Margin="0,18,0,0" BorderBrush="{StaticResource BorderSharp}" BorderThickness="1">
                                <Grid>
                                    <TextBlock Text="PROCESSING TOTAL" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ProcessingCost, StringFormat={}{0:F2}}" FontSize="20" FontWeight="Black" Foreground="{StaticResource WarningOrange}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Actions -->
                    <Border Grid.Column="1" Style="{StaticResource ModuleBorderStyle}" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="ACTIONS" Style="{StaticResource SectionTitle}"/>

                            <TextBlock Text="UNIT" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding UnitOptions}" SelectedValue="{Binding Unit}"/>

                            <TextBlock Text="SEALANT TYPE" Style="{StaticResource FieldLabel}" Margin="0,14,0,0"/>
                            <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding SealantTypes}" SelectedValue="{Binding SealantType}" IsEditable="True"/>

                            <Button Content="💾 SAVE LAMINATION" Command="{Binding SaveCommand}" Style="{StaticResource BtnSuccess}" Margin="0,30,0,10"/>
                            <Button Content="📄 EXPORT PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource BtnPrimary}" Margin="0,0,0,10"/>
                            <Button Content="🗑️ CLEAR" Command="{Binding ClearCommand}" Style="{StaticResource BtnDanger}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- HISTORY -->
                <Border Style="{StaticResource ModuleBorderStyle}" Margin="0,25,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,14">
                            <TextBlock Text="HISTORY" FontSize="14" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                            <Button Content="CLEAR ALL" Command="{Binding ClearAllCommand}" Style="{StaticResource BtnDanger}" Padding="14,8" HorizontalAlignment="Right"/>
                        </Grid>

                        <Border Background="{StaticResource BgLight}" CornerRadius="12" MaxHeight="220" Visibility="{Binding IsHistoryVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" BorderBrush="{StaticResource BorderSharp}" BorderThickness="1">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Records}" Margin="10">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="White" CornerRadius="12" Padding="14" Margin="6" Width="190" BorderBrush="{StaticResource BorderSharp}" BorderThickness="1">
                                                <StackPanel>
                                                    <Grid Margin="0,0,0,8">
                                                        <TextBlock Text="{Binding CreatedAt}" FontSize="9" Foreground="{StaticResource TextMuted}"/>
                                                        <Button Content="✕" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource DeleteBtn}" HorizontalAlignment="Right"/>
                                                    </Grid>
                                                    <TextBlock Text="{Binding DisplayText}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" TextWrapping="Wrap"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                        <Border Background="#FEF3C7" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                                                            <TextBlock FontSize="9" Foreground="{StaticResource WarningOrange}">
                                                <Run Text="W: "/><Run Text="{Binding Wastage}"/>
                                                            </TextBlock>
                                                        </Border>
                                                        <Border Background="#F3E8FF" CornerRadius="6" Padding="8,4">
                                                            <TextBlock FontSize="9" Foreground="#5B21B6">
                                                <Run Text="P: "/><Run Text="{Binding ProfitMargin}"/>
                                                            </TextBlock>
                                                        </Border>
                                                    </StackPanel>
                                                    <Grid Margin="0,10,0,0">
                                                        <TextBlock Text="{Binding Result, StringFormat={}{0:F2}}" FontSize="20" FontWeight="Black" Foreground="{StaticResource SuccessGreen}"/>
                                                        <TextBlock Text="AED" FontSize="10" Foreground="{StaticResource SuccessGreen}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,3"/>
                                                    </Grid>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>

                        <Border Background="{StaticResource BgLight}" CornerRadius="12" Padding="50" Visibility="{Binding IsHistoryEmpty, Mode=OneWay, Converter={StaticResource BoolToVis}}" BorderBrush="{StaticResource BorderSharp}" BorderThickness="1" Height="202">
                            <TextBlock Text="No history yet" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- ADVANCED OPTIONS -->
                <Border Style="{StaticResource ModuleBorderStyle}" Margin="0,25,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="12" Height="12" CornerRadius="6" Background="#EC4899"/>
                            <TextBlock Grid.Column="1" Text="ADVANCED OPTIONS" FontSize="14" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="12,0,0,0"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Edge Work" Style="{StaticResource FieldLabel}"/>
                                <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding EdgeWorkTypes}" SelectedValue="{Binding EdgeWork}" IsEditable="True"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="10,0">
                                <TextBlock Text="Drilling / Holes" Style="{StaticResource FieldLabel}"/>
                                <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding DrillingOptions}" SelectedValue="{Binding Drilling}" IsEditable="True"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="10,0">
                                <TextBlock Text="Heat Treatment" Style="{StaticResource FieldLabel}"/>
                                <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding TemperingOptions}" SelectedValue="{Binding Tempering}" IsEditable="True"/>
                            </StackPanel>

                            <StackPanel Grid.Column="3" Margin="10,0,0,0">
                                <TextBlock Text="Coating" Style="{StaticResource FieldLabel}"/>
                                <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding CoatingTypes}" SelectedValue="{Binding Coating}" IsEditable="True"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Surface Treatment" Style="{StaticResource FieldLabel}"/>
                                <ComboBox Style="{StaticResource SharpCombo}" ItemsSource="{Binding SurfaceTreatments}" SelectedValue="{Binding SurfaceTreatment}" IsEditable="True"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="10,0">
                                <TextBlock Text="Dimensions (mm)" Style="{StaticResource FieldLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Style="{StaticResource SharpInput}" Height="38" Text="{Binding Width, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Column="1" Text="×" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="10,0"/>
                                    <TextBox Grid.Column="2" Style="{StaticResource SharpInput}" Height="38" Text="{Binding Height, UpdateSourceTrigger=LostFocus}"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- QUANTITY & NOTES -->
                <Grid Margin="0,25,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource ModuleBorderStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="QUANTITY &amp; TOTAL" Style="{StaticResource SectionTitle}"/>

                            <TextBlock Text="Quantity" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource SharpInput}" Text="{Binding Quantity, UpdateSourceTrigger=LostFocus}"/>

                            <Grid Margin="0,18,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="TOTAL AREA" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding TotalArea, StringFormat={}{0:F2} m²}" FontSize="18" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="TOTAL PRICE" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:F2}}" FontSize="18" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource ModuleBorderStyle}" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="CUSTOM NOTES" Style="{StaticResource SectionTitle}"/>
                            <TextBox Style="{StaticResource SharpInput}" Height="90" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Text="{Binding CustomNotes, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- COST BREAKDOWN -->
                <Border Style="{StaticResource ModuleBorderStyle}" Margin="0,25,0,0">
                    <StackPanel>
                        <TextBlock Text="COST BREAKDOWN" FontSize="14" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,0,0,18"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="1. Outer Glass Cost" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding OuterPrice, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="2. Inner Glass Cost" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding InnerPrice, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="3. PVB Cost" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding PVBPrice, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="4. Total Glass Cost" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                    <TextBlock Text="{Binding TotalGlassCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextDark}" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="5. Processing Cost" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding ProcessingCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="6. Base Cost (÷ Wastage)" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding BaseCost, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="7. Unit Price (× Profit)" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding Result, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="8. VAT (5%)" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding VatAmount, StringFormat={}{0:F2} AED}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Right"/>
                                </Grid>
                                <Border Background="{StaticResource DeepBlue}" CornerRadius="12" Padding="16,12">
                                    <Grid>
                                        <TextBlock Text="9. GRAND TOTAL" FontSize="14" FontWeight="Black" Foreground="White"/>
                                        <TextBlock Text="{Binding GrossTotal, StringFormat={}{0:F2} AED}" FontSize="16" FontWeight="Black" Foreground="#93C5FD" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- QUICK REFERENCE -->
                <Border Style="{StaticResource ModuleBorderStyle}" Margin="0,25,0,0">
                    <StackPanel>
                        <TextBlock Text="QUICK REFERENCE" FontSize="14" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,0,0,18"/>

                        <UniformGrid Columns="4">
                            <Border Background="{StaticResource BgLight}" CornerRadius="12" Padding="16" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="🔥 HEAT TREATMENT" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                    <TextBlock Text="Annealed, Tempered" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                    <TextBlock Text="Heat Strengthened" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource BgLight}" CornerRadius="12" Padding="16" Margin="10,0">
                                <StackPanel>
                                    <TextBlock Text="🧪 PVB TYPES" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                    <TextBlock Text="Clear 0.76mm, 1.52mm" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                    <TextBlock Text="SentryGlas, Acoustic" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource BgLight}" CornerRadius="12" Padding="16" Margin="10,0">
                                <StackPanel>
                                    <TextBlock Text="📐 TYPICAL THICKNESS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                    <TextBlock Text="6.38mm, 8.38mm" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                    <TextBlock Text="10.38mm, 12.76mm" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource BgLight}" CornerRadius="12" Padding="16" Margin="10,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="🔗 SEALANT TYPES" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                    <TextBlock Text="Butyl, Silicone" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                    <TextBlock Text="Polysulfide" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>